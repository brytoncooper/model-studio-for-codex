from __future__ import annotations

UNVERIFIED_CACHE_CATALOG_SOURCE = "cache"
UNVERIFIED_CACHE_CATALOG_NOTE = (
    "Results come from the local catalog cache. Live endpoint access and model availability are not verified."
)
