# B00–B09 evidence audit

Audit date: 2026-09-12. Read-only source/history inspection in the Architecture worktree; starting HEAD `2ad32a4`. No tests, build, live application, network, or Git mutation was performed. The only authored file is this report. Links are relative to this report. Commit identifiers establish committed implementation, not acceptance or execution success. Concurrent work may supersede this snapshot.

[BACKLOG.md](../BACKLOG.md) supplies the acceptance requirements; current [AGENTS.md](../../../../AGENTS.md) supersedes its older model/wrapper/concurrency rules. In particular, an Editing wrapper is present but is no longer mandatory project policy. No item below is called accepted merely because its tests exist. The current [verification entrypoint](../../../../scripts/verify.py) implements only `development-guard`; all other named gates explicitly return incomplete/unimplemented. No current execution evidence was collected for any gate.

## B00 — Isolation tooling written; staged build operation remains missing

- **Written/committed:** `91d23f4`, `01e8c41`; [guard package](../../../../development/guard), [shared root guard](../../../../python/src/model_deck_root_guard), [Editing wrapper](../../../../scripts/editing_check.py), [worktree recipe](../../../../scripts/recipe_isolated_worktree.sh), and [baseline inventory](../../../../development/inventory.py). Roots are explicit and disjoint; protected paths/symlinks are checked. [Development guide](../../../../development/README.md) documents the supported guard and limitations.
- **Tests present:** [test_development_guard.py](../../../../test_development_guard.py) covers protected bundle/state and aliases; [test_editing_check.py](../../../../test_editing_check.py) covers unsupported input, file limit, process-tree timeout and bounded output. Not executed in this audit.
- **Missing acceptance:** no supported isolated staged app packaging command exists under `scripts/package`; its [README](../../../../scripts/package/README.md) is a packaging proposal. The development guide explicitly says legacy `build.sh` ignores the proposed output variable. Preserve-helper/signature/artifact evidence therefore cannot be inferred from G0 tooling. G0 execution/final review evidence is missing here.
- **Next bounded slice:** implement a separate explicit-output staged packager with collision refusal, keeping installed paths untouched, then verify helper bytes and artifact inventory using isolated outputs. Reuse existing root policy.

## B01 — Shared contracts written; complete freeze acceptance is unverified

- **Written/committed:** `66a65b5` plus later contract amendments, including `8511174`, `dc4eba9`, `6fa45f8`, `2ad32a4`. [Contracts and policy](../../../../contracts/README.md), [API inventory](../API.md), [schemas](../../../../contracts), [generator](../../../../scripts/generate_contracts.py), [Python contracts](../../../../python/src/model_deck_contracts), and [Swift contracts](../../../../macos/Sources/ModelDeckContracts) exist. [Python package](../../../../python/pyproject.toml) pins jsonschema 4.23.0 and tomlkit 0.13.3. Platform-port schemas, operation inventories, plugin manifest/panel shapes, tri-state vocabulary and valid/invalid golden examples are present.
- **Tests present:** [Python contract suite](../../../../python/tests/contracts), including golden fixtures, negotiation, inventory, ports and tool-input schemas; [Swift round trips](../../../../macos/Tests/ModelDeckContractsTests/FixtureRoundTripTests.swift). Fixtures include secret-looking opaque references, tool calls and completed events. Not executed here.
- **Missing acceptance:** no current synchronized generation check, cross-language round-trip result, or consumer-review/freeze record was established. Subsequent changes to contracts mean the original contract commit alone cannot establish the current freeze. The `contracts` verification gate remains unimplemented.
- **Next bounded slice:** wire a contracts gate that verifies inventory/resource synchronization and executes the same fixtures in Python/Swift; record the reviewed contract revision and compatibility policy after that passes. No new API design is justified solely by this audit.

## B02 — Real headless path integrated; current G2 evidence missing

- **Written/committed:** `ed2c2a2` (and `01cd777` later CLI work). [Bootstrap](../../../../python/src/model_deck/bootstrap.py) composes [model use cases](../../../../python/src/model_deck/engine/model_library/use_cases.py), [legacy read adapter](../../../../python/src/model_deck/integrations/hosts/codex/legacy_models.py), injected platform/lock/transport adapters and [CLI](../../../../python/src/model_deck/cli/main.py). Default reads remain legacy; isolated application writes require a flag. This is implementation and composition, not merely ports.
- **Tests present:** [real CLI subprocess fixture](../../../../python/tests/engine/test_engine_cli_subprocess.py), [transport tests](../../../../python/tests/engine/test_engine_transport.py), [CLI failure tests](../../../../python/tests/engine/test_engine_cli.py), and [model use cases](../../../../python/tests/engine/test_model_library.py). Coverage includes real protocol model listing, absent engine, authentication and another session surviving disconnect. Not executed here.
- **Missing acceptance:** current G2 run evidence and a checked end-to-end incompatible-version CLI case; negotiation tests alone do not prove the CLI diagnostic. No additional implementation gap was established in the narrow happy-path bootstrap.
- **Next bounded slice:** connect these focused suites to the engine gate and ensure incompatible handshake is exercised through the CLI entrypoint, preserving the other-client disconnect test.

## B03 — Checker implemented; current package graph requires reconciliation

- **Written/committed:** `f91fc14`; [architecture rules](../../../../development/architecture), [entrypoint](../../../../scripts/architecture_check.py), named legacy baseline and dynamic-import exceptions. The [guide](../../../../development/architecture/README.md) explicitly disclaims runtime sandbox proof.
- **Tests present:** [test_architecture_check.py](../../../../test_architecture_check.py) and [negative/positive fixtures](../../../../development/architecture/fixtures) cover forbidden platform/vendor imports, private imports, service locators and dynamic imports. Not executed here.
- **Missing acceptance:** the current allowed graph is not verified. Source inspection exposes an integration mismatch: [graph.py](../../../../development/architecture/graph.py) recognizes external `model_deck_plugin`/`model_deck_sdk` prefixes but gives no layer to the new `model_deck.plugins.*` supervisor/broker packages; [checker.py](../../../../development/architecture/checker.py) rejects architecture-owned unknown source/target layers. This should not be “fixed” by adding all plugin runtime code to the SDK-only layer without reviewing its role.
- **Next bounded slice:** define the minimum explicit boundary for built-in plugin infrastructure versus external SDK consumers, add positive and negative fixtures for those actual modules, and run the checker over the real tree with migrated checks enforced.

## B04 — Swift package extracted; app artifact acceptance unfinished

- **Written/committed:** Swift package/module extraction is present by `da32d4f`; [Package.swift](../../../../macos/Package.swift), [presentation models](../../../../macos/Sources/ModelDeckPresentation/Models), [geometry](../../../../macos/Sources/ModelDeckPlatform/Geometry) and [app target](../../../../macos/Sources/ModelDeckApp) exist. App target symlinks preserve root source ownership and existing launch/self-test flags, described in its [guide](../../../../macos/Sources/ModelDeckApp/README.md).
- **Tests present:** [browser/usage tests](../../../../macos/Tests/ModelDeckPresentationTests/Models), [geometry tests](../../../../macos/Tests/ModelDeckPlatformTests/Geometry). Not compiled or executed here.
- **Missing acceptance:** explicit staged `.app` packaging and expected assets/helper/signature inventory are not implemented by [scripts/package](../../../../scripts/package). SwiftPM producing an executable alone does not satisfy app resources/helper identity parity. Current compilation and complete mapping of original keychain/companion/browser/usage self-test scenarios are not established.
- **Next bounded slice:** share the B00 staged-packager slice, add executable/assets/helper inventory validation, and map existing self-test flags/scenarios to extracted tests before G3 acceptance. No app launch or installation is needed.

## B05 — Typed Models path wired; real cross-language acceptance remains

- **Written/committed:** `da32d4f`; [root AppKit source](../../../../OpenRouterSettings.swift) constructs `ModelCatalogBootstrap`, owns `ModelCatalogPresenter`, and calls it from `loadCatalog`. [Client/bootstrap/service](../../../../macos/Sources/ModelDeckClient), [main-actor presenter](../../../../macos/Sources/ModelDeckPresentation/Models/ModelCatalogPresenter.swift), [browser state](../../../../macos/Sources/ModelDeckPresentation/Models/ModelBrowserState.swift), and [Unix transport](../../../../macos/Sources/ModelDeckPlatform) are present. Engine selection is explicit via rendezvous; default legacy service and generation guards remain.
- **Tests present:** [presenter tests](../../../../macos/Tests/ModelDeckPresentationTests/Models/ModelCatalogPresenterTests.swift) cover loading/ready/empty/failure/unavailable, cancellation and delayed stale completion; [client tests](../../../../macos/Tests/ModelDeckClientTests) and [transport tests](../../../../macos/Tests/ModelDeckPlatformTests/Transport/UnixSocketEngineTransportTests.swift) cover handshake/framing/socket behavior. Not executed here.
- **Missing acceptance:** no current G2/G3 execution or single Swift client → actual Python fixture engine evidence was established. Fake transport/presenter tests and separate Python socket tests do not themselves prove that composition. Remaining `Process` calls in the root monolith are broader legacy workflows; their existence alone is not evidence that the migrated `loadCatalog` branch starts a process directly.
- **Next bounded slice:** run a staged cross-language catalog fixture and verify the existing five-state/stale-generation suites, recording search/selection/keyboard mapping against retained browser-state tests. Repair only demonstrated gaps.

## B06 — MCP entrypoint converged on engine application operations

- **Implemented:** [model read/write services](../../../../python/src/model_deck/integrations/clients/mcp), presenters, engine transport/resolvers, the named legacy adapter, and [model_deck_mcp.py](../../../../model_deck_mcp.py) environment composition.
- **Tests executed:** adapter/root compatibility tests plus the staged stdio add/list/rename/remove path against the authenticated isolated engine and projection.
- **Integrated evidence:** the unchanged stdio tool names route add, remove, display-name, and registered-model reads through authenticated engine RPC when explicit rendezvous and credential paths are present. The same staged entrypoint was exercised against an isolated engine and its managed host projection. Without both paths, the named legacy path remains selected for rollback compatibility. No MCP-owned registry or host-file write was added.
- **Verdict:** complete for original B06. Benchmark refresh is still owned by B16.

## B07 — Revisioned isolated state integrated; shared repository acceptance incomplete

- **Written/committed:** `6d97031` and subsequent outbox commits. [Model use cases](../../../../python/src/model_deck/engine/model_library/use_cases.py), [connection use cases](../../../../python/src/model_deck/engine/connections/use_cases.py), [SQLite model repository](../../../../python/src/model_deck/adapters/storage/sqlite_model_repository.py), [connection repository](../../../../python/src/model_deck/adapters/storage/sqlite_connection_repository.py), and [bootstrap](../../../../python/src/model_deck/bootstrap.py) wire isolated optional writes. Transactions persist idempotency/outbox records; connection/model identities and credential references are distinct.
- **Tests present:** [SQLite model contracts](../../../../python/tests/engine/test_sqlite_model_repository.py), [SQLite connection contracts](../../../../python/tests/engine/test_sqlite_connection_repository.py), [wire dispatch](../../../../python/tests/engine/test_engine_state_dispatch.py), use-case fake tests, and [registered route resolver](../../../../python/tests/engine/test_registered_route_resolver.py). Tests cover concurrent revision conflicts, replay/payload conflict, secret-column rejection, outbox rollback and retained captured route after removal. Not executed here.
- **New execution evidence:** a deterministic provider run is admitted and held at a tool barrier, its registration is removed through the public socket operation, a new admission is rejected, and the held run completes using its unchanged captured connection/provider route.
- **Missing acceptance:** backlog still asks fake and SQLite to pass identical repository contracts. Existing use-case tests use recording fakes while SQLite has separate behavioral suites; an identical reusable repository conformance suite was not found.
- **Next bounded slice:** factor the meaningful mutation/idempotency/revision cases into a shared repository conformance suite backed by a behaviorally accurate fake and SQLite.

## B08 — Deterministic preview implemented; G4 execution unverified

- **Written/committed:** `cd068de`; [preview package](../../../../python/src/model_deck/integrations/hosts/codex/migration_preview) inventories explicit fixture preferences/endpoints/display names/managed agents, hashes bytes, classifies malformed/foreign/symlink/drift/collision data, and produces redacted deterministic plans without applying them. [Guide](../../../../python/src/model_deck/integrations/hosts/codex/migration_preview/README.md) documents supported shapes and limitations.
- **Tests present:** [preview tests](../../../../python/tests/integrations/hosts/codex/test_migration_preview.py) include repeat serialization across roots, original bytes/hash preservation, versions, drift and identity collisions, symlinks, malformed documents, secret redaction, keyless/Cursor endpoints and display-name source inclusion. Not executed here.
- **Missing acceptance:** current G4 execution evidence. No concrete missing B08 fixture-only behavior was established during source review. This does not establish B11 import/apply/cutover, which is intentionally separate. Secret-field rejection demonstrates no exported value, not OS-level proof that no secret-containing source byte is ever read.
- **Next bounded slice:** add preview tests to the migration gate and record deterministic/redaction/input-preservation acceptance; keep application to the B11 coordinator rather than expanding preview authority.

## B09 — Conflict-aware projection composition complete

- **Written/committed:** `fe19f64`, `0ffe8ed`, `c77aaeb`, `fef8762`, `5d7416b`, `e16e47c`, `46f6eaa`, `cf63246`. [Outbox/intents/receipts](../../../../python/src/model_deck/adapters/storage), [conditional files](../../../../python/src/model_deck/adapters/filesystem/conditional_projection_files.py), [consumer](../../../../python/src/model_deck/integrations/hosts/codex/projection_consumer/consumer.py), [renderer](../../../../python/src/model_deck/integrations/hosts/codex/agent_renderer), and [materializer](../../../../python/src/model_deck/integrations/hosts/codex/agent_materializer) implement managed writes/deletes, hash preconditions, durable intent and separate applied revisions/tombstones. Materializer now calls the real renderer; consumer README's “no real renderer” description is stale at system level.
- **Tests executed:** [consumer tests](../../../../python/tests/integrations/hosts/codex/test_projection_consumer.py), [renderer](../../../../python/tests/integrations/hosts/codex/test_agent_renderer.py), [materializer](../../../../python/tests/integrations/hosts/codex/test_agent_materializer.py), SQLite/file-port suites, and [full-path composition](../../../../python/tests/integrations/hosts/codex/test_projection_full_path.py).
- **Integrated evidence:** bootstrap composes the existing committed snapshots, outbox, dependency recovery, materializer, conditional file adapter, intents, and receipts behind one host coordinator. Successful model/connection mutations trigger best-effort delivery only after commit; status reports `ready`, `pending`, or persisted `failed` without changing the mutation result. Atomic `connection.saved` expansion re-drives only affected registrations.
- **Acceptance:** isolated full-path tests cover create, stable-identity rename, connection fanout, owned deletion, idempotent restart, staged MCP mutation, foreign-file refusal, conflict persistence across restart, and recovery after the user removes the foreign file and submits a new public revision.
- **Verdict:** complete for original B09. Actual Codex compatibility/reload and migration cutover remain B10/B11.
