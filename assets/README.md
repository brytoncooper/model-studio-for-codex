# Model Studio icon

`ModelStudio.png` is the source artwork. `build-icon.sh` produces the macOS ICNS sizes using sips and iconutil, preserving alpha. The app build embeds both assets.

Generated and revised with the built-in image-generation tool on 2026-09-11. User-approved final design: a dark charcoal macOS rounded-square tile with two overlapping offset cards, a larger slate-blue workspace and smaller teal companion panel. Bold asymmetric silhouette, subtle satin depth, no letterforms, rotating interlocks, hooked arms, text, or existing company logos. Final extraction prompt: preserve the foreground icon and remove the external checkerboard background using real PNG alpha transparency.
